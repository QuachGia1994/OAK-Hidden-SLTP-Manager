# OAK workspaces
> updated 2026-09-08 · v4.1.0

The six-screen concept defines visual hierarchy; live prices, dates, cards and AI results always come from the existing product.

## Routes and contracts
- /engine: unified Live + retained-history broker-day matrix for XAUUSD/GBPUSD/GBPAUD/GBPCAD/GBPJPY across H3/H6/H9/H12/H14/H16. Rule v88 keeps XAUUSD as entry-time owner; XAUUSD/GBPAUD/GBPCAD/GBPJPY show BUY/SELL from previous available broker-day GBPUSD/AUDUSD/USDCAD/USDJPY H(entry-2), while GBPUSD keeps same-day own H(entry-2). Evidence labels mapped bases `PREV H(entry-2)`, H16 remains entry-only, and the XAU H3/H14 H4-entry highlight stays presentation-only.
- /history: compatibility route only; redirects to `/engine`, where the same Sunday-first retained-date calendar is available as a compact popover.
- /neotech: visible optional C5 reminder EA v1.08 download with checksum, bilingual install/controller guide and explicit same-PC Telegram prerequisites. The helper auto-binds the active MT5 login/server across account switches and exposes no account/token input; the UI separates existing-controller setup from new-PC bootstrap and links the canonical local-failover guide. The rule-evaluation workspace keeps its compact hero, existing MT5 connection disclosure, grouped criteria and clearly marked sample results; rule thresholds and account actions remain unchanged.
- /tools: three illustrated tool cards with localized categories and clear destination links, backed by the shared OAK_TOOLS catalog. Cards sit side by side on desktop and stack on smaller screens.
- /factcheck: text/link and image drafts survive mode switches. OCR returns editable text; image analysis preserves origin, generation, manipulation and completeness. Public sharing and backend handlers are unchanged.
- /tarot: compact question and one/three-card controls beside the real 78-card deck on desktop and above it on mobile. Decorative backs never replace the server draw. AI failure still preserves drawn cards.
- /discover: Daily across the top, then Dream/Oracle and Mood/Compatibility. Daily, Oracle and Mood stay local; only Dream/Compatibility call AI.

## Presentation owners
- globals.css owns semantic tokens and workspace width. All six workspaces (/engine, /neotech, /tools, /factcheck, /tarot and /discover) share graphite/mint through route-scoped command palette tokens. Shared-report and account routes retain their existing palette. Light and contrast remain available.
- oak-redesign.css owns navigation, WorkspaceHeading, ToolArtwork, workspace layout and image-input composition. Replaced367old selectors before adding the canonical rules. factcheck-share.css retains report/share semantics; overlapping image-input declarations were moved to their composition owner.
- EngineCore owns the localized /engine heading, saved-feed status, publication time, metadata and decorative CSS depth illustration. Missing data is shown explicitly; the illustration does not represent order execution or profit. H1EngineBoard retains the existing signal table and adds links to NeoTech and broker accounts. ToolsClient owns the localized tools directory heading and cards in tools.module.css; NeoTechPublicDashboard owns its rule workspace in neotech.module.css. Fact Check, Tarot and Discover each own a scoped workspace stylesheet beside their components. Fact Check has an illustrated heading and text/image workbench; Tarot places its question form beside the deck on desktop; Discover features Daily above its other four experiences and reuses WorkspaceHeading. NavBar uses the real oak-app-icon.png and OAK GATEKEEPER wordmark. Mobile has three direct tabs; desktop retains a keyboard-operable tools menu. Directory links use flex layout, never the icon/detail grid.
- ToolArtwork's finite kind maps to one generated transparent3x3atlas, dashboard/public/oak-workspace-atlas-v2.webp (489488bytes, SHA256 df9d9969b7e9fe908123ea053ebf7867490461413454dfef870308fb9db7fa73). Built-in ImageGen prompt: premium luminous navy/cyan/violet/gold painterly3D sprite sheet; photo+magnifier, fanned tarot, compass; mountain sunrise, crescent clouds, oracle sphere; crystal hearts, ornate single card back, night clouds. No text, labels or data. WebP is a format conversion of the generated original. This is concept-directed new artwork, not pixel-identical extracted art.
- Browser follow-up removed legacy HUD glass overrides from workspace panels, restored cyan borders, fitted all six H1 columns at393px (native pan retained at320px), and reduced Discover mobile header/form height. Tarot backs use centered artwork within the deck panel.
- H1EvidencePanel retains its dialog variant with focus trapping; inline variant is a normal region. Copy chart uses the same renderer.
- /workspace-review.html is a noindex visual QA harness: same-origin real pages at320/393/768/1100px widths. It introduces no mock data or API. This tests responsive layout in Chromium, not iOS Safari engine behavior.

## Verification
- Full dashboard test suite passes325/325 after the workspace redesign. Browser checks cover desktop1440px and mobile320px, EN/VN, theme switching, tool navigation, input state and the MT5 connection disclosure. Live trading/account mutations and paid AI requests were not exercised.
-Source assertions updated for direct mobile navigation, single H1 header and inline evidence; trading/backend assertions retained.
- Local browser screenshots and interaction checks are recorded in the shared task plan; production deployment was not verified.
-No literal100%pixel-parity or AI health claim follows from unit tests. Concept has illustrative results and sample signals that must never be fabricated in production.
