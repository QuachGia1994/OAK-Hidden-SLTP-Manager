{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the OAK Manager main window. Strict: no arbitrary shell, no fs, no http.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
