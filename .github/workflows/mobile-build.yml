name: Mobile Build

on:
  push:
    branches: [main, master]
    paths:
      - "mobile/**"
      - "dashboard/src/app/api/mobile/**"
      - "dashboard/src/lib/h1-signals.ts"
      - "dashboard/src/lib/h1-signals.test.ts"
      - ".github/workflows/mobile-build.yml"
  pull_request:
    branches: [main, master]
    paths:
      - "mobile/**"
      - "dashboard/src/app/api/mobile/**"
      - "dashboard/src/lib/h1-signals.ts"
      - "dashboard/src/lib/h1-signals.test.ts"
      - ".github/workflows/mobile-build.yml"
  workflow_dispatch:

permissions:
  contents: read

env:
  CI: "1"
  EXPO_NO_TELEMETRY: "1"

jobs:
  verify-mobile:
    name: Verify mobile bundle
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.22.0
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.14.0"
          cache: pnpm
          cache-dependency-path: mobile/pnpm-lock.yaml

      - name: Install mobile dependencies
        working-directory: mobile
        run: pnpm install --frozen-lockfile

      - name: Typecheck mobile
        working-directory: mobile
        run: pnpm typecheck

      - name: Bundle Android JavaScript
        working-directory: mobile
        run: pnpm bundle:android

      - name: Bundle iOS JavaScript
        working-directory: mobile
        run: pnpm bundle:ios

  build-android:
    name: Build Android APK
    needs: verify-mobile
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.22.0
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.14.0"
          cache: pnpm
          cache-dependency-path: mobile/pnpm-lock.yaml

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android API 36
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v4

      - name: Install mobile dependencies
        working-directory: mobile
        run: pnpm install --frozen-lockfile

      - name: Generate Android native project
        working-directory: mobile
        run: pnpm exec expo prebuild --platform android --clean --no-install

      - name: Build debug APK
        working-directory: mobile/android
        run: |
          chmod +x gradlew
          ./gradlew :app:assembleDebug --no-daemon

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: OAK-Gatekeeper-Android-debug
          path: mobile/android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14

  build-ios:
    name: Build unsigned iOS IPA
    needs: verify-mobile
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.22.0
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.14.0"
          cache: pnpm
          cache-dependency-path: mobile/pnpm-lock.yaml

      - name: Install mobile dependencies
        working-directory: mobile
        run: pnpm install --frozen-lockfile

      - name: Generate iOS native project
        working-directory: mobile
        run: pnpm exec expo prebuild --platform ios --clean --no-install

      - name: Install CocoaPods
        working-directory: mobile/ios
        run: pod install

      - name: Build unsigned iPhoneOS app and package IPA
        working-directory: mobile/ios
        shell: bash
        run: |
          set -euo pipefail
          WORKSPACE=$(find . -maxdepth 1 -name '*.xcworkspace' -print -quit)
          if [ -z "$WORKSPACE" ]; then
            echo "No Xcode workspace generated" >&2
            exit 1
          fi
          SCHEME=$(xcodebuild -list -json -workspace "$WORKSPACE" | python3 -c 'import json,sys; d=json.load(sys.stdin); s=d.get("workspace",{}).get("schemes",[]); print(next((x for x in s if not x.startswith("Pods-")), ""))')
          if [ -z "$SCHEME" ]; then
            echo "No app scheme found" >&2
            exit 1
          fi
          xcodebuild \
            -workspace "$WORKSPACE" \
            -scheme "$SCHEME" \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY='' \
            DEVELOPMENT_TEAM='' \
            build
          APP_PATH=$(find build/Build/Products/Release-iphoneos -maxdepth 1 -name '*.app' -print -quit)
          if [ -z "$APP_PATH" ]; then
            echo "No unsigned iPhoneOS app produced" >&2
            exit 1
          fi
          rm -rf ipa-payload
          mkdir -p ipa-payload/Payload
          ditto "$APP_PATH" "ipa-payload/Payload/$(basename "$APP_PATH")"
          find ipa-payload/Payload -type d -name '_CodeSignature' -prune -exec rm -rf {} +
          find ipa-payload/Payload -name 'embedded.mobileprovision' -delete
          (
            cd ipa-payload
            /usr/bin/zip -qry ../OAK-Gatekeeper-unsigned.ipa Payload
          )
          test -s OAK-Gatekeeper-unsigned.ipa

      - name: Upload unsigned iOS IPA
        uses: actions/upload-artifact@v4
        with:
          name: OAK-Gatekeeper-iOS-unsigned
          path: mobile/ios/OAK-Gatekeeper-unsigned.ipa
          if-no-files-found: error
          retention-days: 14
