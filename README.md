# 🚀 OAK Hidden SLTP Manager v2.8.7

## ✅ Chức năng chính
- Quản lý SL/TP ẩn và tự đóng lệnh theo rủi ro/lợi nhuận.
- Tính lot theo rủi ro (% balance hoặc số tiền), hỗ trợ vàng.
- Copy trade Master/Slave, stealth, giới hạn 1 lệnh/symbol, bỏ qua symbol.
- Hẹn giờ vào lệnh, tự đóng ngược chiều khi kích hoạt.
- Điều khiển từ xa qua Telegram và hỗ trợ ngôn ngữ tự nhiên.

## � Lưu ý cá nhân
- `profiles.json`, `settings.json`, `trades.json`, `tele_*` là dữ liệu cá nhân.
- Không chia sẻ `OAK_Profile_Backup.zip` ra ngoài.
- Khi đổi máy, cập nhật đường dẫn `terminal64.exe` trong `profiles.json`.

## �️ Cài máy mới (ngắn gọn)
1. Giải nén `OAK_Source_v2.8.7_Clean.zip`.
2. Cài Python 3.x và chạy `pip install -r requirements.txt`.
3. (Tuỳ chọn) Giải nén `OAK_Profile_Backup.zip` để giữ cấu hình.
4. Mở `profiles.json` và chỉnh `terminal64.exe` đúng máy mới.
5. Chạy `CHAY_ROBOT.bat`.

## 📞 Liên hệ hỗ trợ
- Tác giả: Quách Kim Phong
- Telegram: [@bupbupchot](https://t.me/bupbupchot)
